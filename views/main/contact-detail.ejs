<%- include('../includes/head.ejs') %>
  </head>
  <body>
    <main>
      <article>
        <header>
          <h2><%= contact.surname %>, <%= contact.name %></h2>
        </header>
          <p><%= contact.type %></p>
          <p><%= contact.group %></p>
          <p><%= contact.telefon %></p>
          <p><%= contact.email %></p>
          <a href="/edit-contact/<%= contact._id %>">Edit</a>
      </article>
    </main>
  </body>
</html>
